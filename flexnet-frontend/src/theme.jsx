import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#5391f5',
            light: '#DCEEFA'
        }
    }
})

export default theme;